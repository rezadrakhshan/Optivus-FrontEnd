import {
    BrowserRouter,
    Routes,
} from "react-router-dom";



export default function AppRoutes() {
    return (
        <BrowserRouter>
            <Routes>
            </Routes>
        </BrowserRouter>
    );
}